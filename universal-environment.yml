name: sauder-canvas-api
channels:
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - python>=3.7.0
  - python-dotenv
  - reportlab # only used in quiz-reports
  - termcolor
  - requests
  - jupyter
  - pandas
  - pip
  - pip:
      - canvasapi
      - pick
      - art # only used in setup-test
